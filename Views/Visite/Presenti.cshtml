@model List<RegistroVisitatori.Models.Visita>

@{
    ViewData["Title"] = "Visitatori presenti";
}


<h1 class="mb-4">Visitatori presenti</h1>


<p>
    <a class="btn btn-primary" asp-action="Nuovo">
        <i class="bi bi-person-plus"></i> Nuovo visitatore
    </a>

</p>

@if (!Model.Any())
{
    <p>Nessun visitatore presente.</p>
}
else

{
    <p class="text-muted">
    Elenco dei visitatori attualmente presenti nella struttura.
</p>
    <table class="table table-striped table-hover align-middle">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Azienda</th>
            <th></th>
            <th>Ora ingresso</th>
            <th>Stato</th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var v in Model)
            {
                <tr>
                    <td>@v.Nome @v.Cognome</td>
                    <td>@v.Azienda</td>

                    <td>
                        <button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal"
                            data-bs-target="#dettagliModal-@v.Id">
                            Dettagli
                        </button>
                    </td>

                    <td>@v.OraIngresso.ToString("HH:mm")</td>

                    <td>
                        <span class="badge bg-success">Presente</span>
                    </td>

                    <td>
                        <form asp-action="RegistraUscita" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@v.Id" />
                            <button class="btn btn-sm btn-outline-danger" type="submit">
                                Registra uscita
                            </button>
                        </form>

                        <button type="button" class="btn btn-sm btn-outline-secondary btn-delete ms-2" data-id="@v.Id"
                            data-returnto="Presenti" title="Elimina">
                            <i class="bi bi-trash"></i>
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Aurea Services - Conferma eliminazione</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Chiudi"></button>
            </div>

            <div class="modal-body">
                @if (TempData["DeleteError"] != null)
                    {
                        <div class="alert alert-danger mb-3">
                            @TempData["DeleteError"]
                        </div>
                    }
                    <p>
                        Stai per eliminare definitivamente questa visita dal registro aziendale.<br />
                        L’operazione non è reversibile.
                    </p>

                    <div class="mb-2">
                        <label class="form-label">Password di conferma</label>
                        <input type="password" class="form-control" id="deletePassword" required />
                        <div class="form-text">Inserisci la password per confermare l’eliminazione.</div>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Annulla</button>

                    <form id="deleteForm" asp-action="Elimina" method="post" class="m-0">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" id="deleteId" />
                        <input type="hidden" name="returnTo" id="deleteReturnTo" value="Presenti" />
                        <input type="hidden" name="password" id="deletePasswordHidden" />
                        <button type="submit" class="btn btn-danger">Elimina</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

}
@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const deleteModalEl = document.getElementById('deleteModal');
            if (!deleteModalEl) return;

            const modal = new bootstrap.Modal(deleteModalEl);

            document.querySelectorAll('.btn-delete').forEach(btn => {
                btn.addEventListener('click', function () {
                    document.getElementById('deleteId').value = this.getAttribute('data-id');
                    document.getElementById('deleteReturnTo').value = this.getAttribute('data-returnto');

                    document.getElementById('deletePassword').value = '';
                    document.getElementById('deletePasswordHidden').value = '';

                    modal.show();
                });
            });

            document.getElementById('deleteForm').addEventListener('submit', function () {
                document.getElementById('deletePasswordHidden').value =
                    document.getElementById('deletePassword').value;
            });

            // Riapertura automatica se password errata
        @if (TempData["OpenDeleteModal"]?.ToString() == "1")
        {
            <text>
                        document.getElementById('deleteId').value = '@TempData["DeleteId"]';
                        document.getElementById('deleteReturnTo').value = '@TempData["DeleteReturnTo"]';
                        modal.show();
            </text>
        }
        });
    </script>
}
